# FlakeGuard

FlakeGuard tells you which of your failing tests are actually broken and which ones are just flaky, then quarantines the flaky ones automatically so they stop blocking merges while still getting tracked.

More detail on the architecture and setup goes here as the project fills out.
